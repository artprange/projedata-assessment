type ProductFormStateTypes = {
	code: string;
	name: string;
	price: string;
};

type AddMaterialStateTypes = {
	materialId: string;
	requiredQuantity: string;
};

export type { ProductFormStateTypes, AddMaterialStateTypes };
